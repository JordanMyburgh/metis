# Next steps

- Already have a folder of markdown notes? Re-point Metis at it:
  `node setup.mjs --vault "C:\path\to\your\notes" --start`
- Or keep using this folder as your vault — just add `.md` files.
- `--hooks` and `--startup` (see SETUP.md) make Metis permanent: start at login,
  attach live to new Claude Code sessions.
- These four starter notes are yours to edit or delete.
